{% extends 'admin/layouts/base_admin.twig' %}
{% block title %}Novo Mandato{% endblock %}

{% block content %}
<div class="container-fluid px-3 px-md-4 py-4">
    <div class="mb-4">
        <h2 class="fw-bold" style="color: var(--admin-primary);"><i class="bi bi-clock-history me-2"></i>Cadastrar mandato</h2>
        <p class="text-muted mb-0">Cadastre o período de gestão e use esse vínculo na composição da diretoria.</p>
        <hr class="my-4">
    </div>

    <form method="post" action="{{ BASE_URL }}admin/mandatos/salvar">
        <input type="hidden" name="csrf_token" value="{{ csrf_token|e }}">
        <div class="row g-3">
            <div class="col-lg-6">
                <label class="form-label fw-bold">Nome do mandato</label>
                <input type="text" name="nome" class="form-control" required placeholder="Ex.: Gestão 2026-2028">
            </div>
            <div class="col-lg-3">
                <label class="form-label fw-bold">Data de início</label>
                <input type="date" name="data_inicio" class="form-control" required>
            </div>
            <div class="col-lg-3">
                <label class="form-label fw-bold">Data final</label>
                <input type="date" name="data_fim" class="form-control">
            </div>
            <div class="col-lg-3">
                <label class="form-label fw-bold">Status</label>
                <select name="status" class="form-select">
                    {% for option in statusOptions %}
                        <option value="{{ option }}" {% if option == 'planned' %}selected{% endif %}>{{ option|title }}</option>
                    {% endfor %}
                </select>
            </div>
            <div class="col-12">
                <label class="form-label fw-bold">Observações</label>
                <textarea name="observacoes" rows="5" class="form-control"></textarea>
            </div>
        </div>
        <div class="border-top my-4"></div>
        <div class="d-flex flex-column flex-md-row gap-2 justify-content-md-end">
            <a href="{{ BASE_URL }}admin/mandatos" class="btn btn-outline-secondary">Cancelar</a>
            <button type="submit" class="btn btn-success">Salvar mandato</button>
        </div>
    </form>
</div>
{% endblock %}
