<footer class="border-top" style="background: linear-gradient(180deg, #ffffff, #f8f9fa); border-color: rgba(223,99,1,0.1) !important; transition: margin 0.35s cubic-bezier(0.4, 0, 0.2, 1), width 0.35s cubic-bezier(0.4, 0, 0.2, 1);">
    <div class="container-fluid py-3 d-flex justify-content-between align-items-center">
        <div class="mt-auto p-3 small text-center">
            <i class="bi bi-fire" style="color: #df6301;"></i>
            <span class="small fw-medium" style="color: #212529;">© {{ 'now'|date('Y') }} Corpo de Bombeiros Militar do RN</span>
        </div>
        <div class="mt-auto p-3 small text-center">
            <div class="d-flex align-items-center justify-content-center gap-2 mb-2">
                <i class="bi bi-shield-fill-check"></i>
                <span class="fw-semibold">CBMRN</span>
            </div>
            <span class="small fw-medium">© {{ 'now'|date('Y') }} Portal Administrativo</span>
        </div>
    </div>
</footer>
