{% extends 'admin/layouts/base_admin.twig' %}
{% block title %}Editar Categoria - Admin{% endblock %}
{% block content %}
<div class="container-fluid px-3 px-md-4 py-4">
    <div data-aos="fade-down" class="mb-4">
        <h2 class="fw-bold" style="color: var(--admin-primary);"><i class="bi bi-tags me-2"></i>Editar categoria</h2>
        <p class="text-muted mb-0">Atualize as informações desta categoria de post.</p>
    </div>
    <hr class="my-4">
    <form method="post" action="{{ BASE_URL }}admin/post-categories/update/{{ categoria.id }}">
        <input type="hidden" name="csrf_token" value="{{ csrf_token|e }}">
        <div class="row g-3">
            <div class="col-md-8">
                <label class="form-label fw-bold">Nome da categoria</label>
                <input type="text" class="form-control" name="nome" value="{{ categoria.nome }}" required data-slug-source>
            </div>
            <div class="col-md-4">
                <label class="form-label fw-bold">Slug</label>
                <input type="text" class="form-control text-lowercase" name="slug" value="{{ categoria.slug }}" data-slug-target maxlength="140">
                <div class="form-text">Atualize o identificador da URL, se necessário.</div>
            </div>
            <div class="col-md-4">
                <label class="form-label fw-bold">Cor do badge</label>
                <div class="d-flex gap-2 align-items-center color-picker-wrapper" data-color-picker>
                    <input type="color"
                           class="form-control form-control-color flex-shrink-0"
                           name="badge_color"
                           value="{{ categoria.badge_color|default('#df6301') }}"
                           title="Escolha a cor do badge"
                           data-color-input
                           required>
                    <input type="text"
                           class="form-control text-uppercase"
                           value="{{ categoria.badge_color|default('#df6301') }}"
                           maxlength="7"
                           data-color-hex
                           placeholder="#DF6301">
                </div>
                <div class="form-text">Selecione ou informe um valor hexadecimal (#RRGGBB).</div>
            </div>
            <div class="col-12">
                <label class="form-label fw-bold">Descrição</label>
                <textarea name="descricao" rows="4" class="form-control" placeholder="Descrição breve (opcional)">{{ categoria.descricao }}</textarea>
            </div>
        </div>
        <div class="border-top my-4"></div>
        <div class="d-flex flex-column flex-md-row gap-2 justify-content-md-end">
            <a href="{{ BASE_URL }}admin/post-categories" class="btn btn-outline-secondary"><i class="bi bi-arrow-left me-1"></i> Voltar</a>
            <button type="submit" class="btn btn-warning"><i class="bi bi-save me-1"></i> Atualizar</button>
        </div>
    </form>
</div>
{% endblock %}

{% block scripts %}
    {{ parent() }}
    <script>
        document.querySelectorAll('[data-color-picker]').forEach(function(wrapper) {
            const colorInput = wrapper.querySelector('[data-color-input]');
            const hexInput = wrapper.querySelector('[data-color-hex]');
            if (!colorInput || !hexInput) return;

            const normalize = (value) => {
                const upper = value.toUpperCase();
                return /^#([A-F0-9]{6})$/.test(upper) ? upper : null;
            };

            const syncFromColor = () => {
                hexInput.value = colorInput.value.toUpperCase();
            };

            const syncFromHex = () => {
                const normalized = normalize(hexInput.value.trim());
                if (normalized) {
                    colorInput.value = normalized;
                    hexInput.value = normalized;
                } else {
                    hexInput.value = colorInput.value.toUpperCase();
                }
            };

            colorInput.addEventListener('input', syncFromColor);
            hexInput.addEventListener('blur', syncFromHex);
        });

        (function() {
            const source = document.querySelector('[data-slug-source]');
            const target = document.querySelector('[data-slug-target]');
            if (!source || !target) {
                return;
            }

            let userEdited = target.value.trim().length > 0;

            const slugify = (value) => {
                return value
                    .normalize('NFD')
                    .replace(/[\u0300-\u036f]/g, '')
                    .replace(/[^a-zA-Z0-9]+/g, '-')
                    .replace(/^-+|-+$/g, '')
                    .toLowerCase();
            };

            target.addEventListener('input', () => {
                userEdited = target.value.trim().length > 0;
            });

            source.addEventListener('input', () => {
                if (userEdited) {
                    return;
                }
                target.value = slugify(source.value);
            });
        })();
    </script>
{% endblock %}
